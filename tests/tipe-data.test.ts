describe('Data Type', function () {
    it('should must declare', () => {

        const name: string = "Hadid Hardiansyah Saputra";
        const balance: number = 100;
        const isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip)

    });
})