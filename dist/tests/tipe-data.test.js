"use strict";
describe('Data Type', function () {
    it('should must declare', () => {
        const name = "Hadid Hardiansyah Saputra";
        const balance = 100;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
